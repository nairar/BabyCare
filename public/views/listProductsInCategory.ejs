<ul class="productCards" ng-repeat="product in products">
				<li>
					<div>
						<img src="{{product.item.thumbnailImage}}" />
					</div>
					<div>
						{{product.item.name}}
					</div>
				</li>
</ul>